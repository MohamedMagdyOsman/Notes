## 1️⃣ Absolute Path vs Relative Path

### 🔹 Absolute Path
- Starts from the root directory `/`
- Always the same, regardless of current location

Example:
```bash
/etc/passwd
/home/user/.ssh/id_rsa
````

---

### 🔹 Relative Path

* Based on the current working directory
* Does NOT start with `/`

Example:

```bash
./script.sh
../config.txt
```

Special symbols:

* `.`  → current directory
* `..` → parent directory


## 2️⃣ Hidden Files in Linux

* Hidden files start with a **dot (`.`)**
* Not shown by default

Examples:

```bash
.bashrc
.gitconfig
.ssh/
```

Show hidden files:

```bash
ls -a
```

## 3️⃣ Directory & File Creation

### 🔹 `mkdir` — Create Directories

```bash
mkdir test
mkdir dir1 dir2
mkdir -p a/b/c
```

Important option:

* `-p` → create parent directories automatically

---

### 🔹 `ls -R` — Recursive Listing

```bash
ls -R
```

Lists files and subdirectories recursively.

## 4️⃣ Creating Files (Different Methods)

### 🔹 `touch` — Create Empty File

```bash
touch file.txt
```

Also used to update timestamps.

---

### 🔹 `cat` — Create File with Content

```bash
cat > file.txt
Hello
Ctrl+D
```

---

### 🔹 Redirection (`>`)

```bash
echo "test" > file.txt
```

## 5️⃣ Viewing File Contents

### 🔹 `cat` — Display Entire File

```bash
cat file.txt
```

Best for small files.

---

### 🔹 `more` — Paged Viewing (forward only)

```bash
more file.txt
```

---

### 🔹 `less` — Advanced Pager (recommended)

```bash
less file.txt
```

Features:

* Scroll forward/backward
* Search with `/`

---

### 🔹 `head` — First Lines

```bash
head file.txt
head -n 20 file.txt
```

---

### 🔹 `tail` — Last Lines

```bash
tail file.txt
tail -f /var/log/syslog
```

🔐 **Pentesting relevance:**
`tail -f` is used for live log monitoring.

## 6️⃣ Checking Disk Space & Usage

### 🔹 `df` — Disk Free Space

```bash
df -h
```

Shows filesystem usage.

---

### 🔹 `du` — Disk Usage

```bash
du -h file.txt
du -sh directory/
```

## 7️⃣ Copying & Moving Files

### 🔹 `cp` — Copy Files or Directories

```bash
cp file1 file2
cp -r dir1 dir2
```

Important option:

* `-r` → recursive copy

---

### 🔹 `mv` — Move or Rename

```bash
mv file1 /tmp/
mv old.txt new.txt
```

## 8️⃣ Removing Files & Directories

### 🔹 `rm` — Remove Files

```bash
rm file.txt
rm -r directory/
rm -rf directory/
```

⚠️ **Danger:**
`rm -rf` deletes without confirmation.

---

### 🔹 `rmdir` — Remove Empty Directories

```bash
rmdir empty_dir
```

Only works if directory is empty.

## 9️⃣ Basic Regular Expressions (Regex)

Regex is used for **pattern matching** in commands like `ls`, `grep`, and `find`.

### 🔹 `*` — Zero or More Characters

```bash
ls *.txt
```

Matches:

* `a.txt`
* `notes.txt`

---

### 🔹 `?` — Exactly One Character

```bash
ls file?.txt
```

Matches:

* `file1.txt`
* `fileA.txt`

---

### 🔹 `[ ]` — Character Set

```bash
ls file[123].txt
```

Matches:

* `file1.txt`
* `file2.txt`

## ✅ Key Takeaways

* Absolute paths start from `/`
* Relative paths depend on current directory
* Hidden files start with `.`